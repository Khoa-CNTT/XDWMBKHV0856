import ChatBox from "./ChatBox";

/**
 * AI Chat wrapper component to be used in the application
 * This component can be used anywhere in the app to provide access to the AI chat
 */
const AiChat = () => {
  return <ChatBox />;
};

export default AiChat;
